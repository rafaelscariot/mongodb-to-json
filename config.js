/* 
    DB_NAME -> database name 
    DOCUMENTS_LIMIT -> defines the maximum number of documents from each collection that will be saved
*/

module.exports = { DB_NAME: "my_database", DOCUMENTS_LIMIT: 100 };
